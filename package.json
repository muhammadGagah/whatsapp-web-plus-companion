{
  "name": "whatsapp-web-plus-companion",
  "version": "2026.09.23",
  "license": "GPL-2.0-or-later",
  "private": true,
  "scripts": {
    "sync:userscript": "node scripts/sync-userscript.mjs",
    "test:python": "uv run python -m unittest discover -s tests -v",
    "lint": "uv run ruff check . && uv run ruff format --check .",
    "build": "uv run scons && uv run python scripts/verify-registry-helper.py",
    "test": "npm run test:python && npm run lint && npm run build"
  }
}
